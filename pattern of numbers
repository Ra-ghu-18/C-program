#include<stdio.h>
#include<conio.h>
void main()
{
int row,space,i,count=1,j;
clrscr();
printf("enter no of rows:");
scanf("%d",&row);
for(i=1;i<row,i++)
{
for(space=1;space<=row-i;space++)
{
printf(" ");
}
for(j=1;j<=i;j++)
{
printf("%d ",count++);
}
printf("\n");
}
getch();
}
